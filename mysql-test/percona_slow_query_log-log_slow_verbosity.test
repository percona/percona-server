source include/have_innodb.inc;

SELECT sleep(2);

set log_slow_verbosity=innodb;

SELECT sleep(2);

let $cmd = cat var/mysqld.1/data/percona_slow_query_log.log | grep InnoDB | wc -l;
echo $cmd;
exec $cmd;
