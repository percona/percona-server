# This is a wrapper for binlog_drop_tbl_with_comment.test so that the same
# test case can be used ror both statement and row based bin logs

-- source include/have_binlog_format_mixed_or_statement.inc
-- source extra/binlog_tests/binlog_drop_tbl_with_comment.test
