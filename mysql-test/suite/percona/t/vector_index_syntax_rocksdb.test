--source include/have_rocksdb.inc

CREATE TABLE t1 (
  a CHAR( 2 ),
  KEY ( a(1) )
) ENGINE = ROCKSDB;

--echo # Ensure the index type is SE_SPECIFIC and doesn't erroneously show up a VECTOR
SHOW KEYS FROM t1;

DROP TABLE t1;
