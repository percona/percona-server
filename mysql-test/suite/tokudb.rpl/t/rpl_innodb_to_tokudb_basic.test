--source include/master-slave.inc
--source include/have_innodb.inc
--source include/have_tokudb.inc

let $master_engine=innodb;
let $slave_engine=tokudb;

--source suite/tokudb.rpl/include/rpl_tokudb_innodb.inc
