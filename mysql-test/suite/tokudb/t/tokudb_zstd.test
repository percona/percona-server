--source include/have_tokudb.inc
--source include/have_innodb.inc

CREATE TABLE t2 (c1 INT, c2 INT) ENGINE = TOKUDB ROW_FORMAT=TOKUDB_SNAPPY;
SHOW CREATE TABLE t2;
INSERT INTO t2 VALUES (1,2),(3,4),(5,6);

# alter
ALTER TABLE t2 ROW_FORMAT=TOKUDB_DEFAULT;
SHOW CREATE TABLE t2;
SELECT * FROM t2 ORDER BY c1;

ALTER TABLE t2 ENGINE=TOKUDB ROW_FORMAT=TOKUDB_DEFAULT;
SHOW CREATE TABLE t2;
FLUSH TABLES;
SELECT * FROM t2 ORDER BY c2;

# cleanup
DROP TABLE t2;
