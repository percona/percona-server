
SELECT @@global.audit_log_filter_flush;

SET GLOBAL audit_log_filter_flush=OFF;
SELECT @@global.audit_log_filter_flush;

SET GLOBAL audit_log_filter_flush=ON;
SELECT @@global.audit_log_filter_flush;

--error ER_WRONG_VALUE_FOR_VAR
SET GLOBAL audit_log_filter_flush='';

--error ER_WRONG_VALUE_FOR_VAR
SET GLOBAL audit_log_filter_flush='str';

--error ER_WRONG_TYPE_FOR_VAR
SET GLOBAL audit_log_filter_flush=0.5;
