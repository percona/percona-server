
SET GLOBAL audit_log_filter_rotations=0;
SELECT @@global.audit_log_filter_rotations;

SET GLOBAL audit_log_filter_rotations=1;
SELECT @@global.audit_log_filter_rotations;

SET GLOBAL audit_log_filter_rotations=999;
SELECT @@global.audit_log_filter_rotations;

SET GLOBAL audit_log_filter_rotations=1000;
SELECT @@global.audit_log_filter_rotations;

SET GLOBAL audit_log_filter_rotations=-1;
SELECT @@global.audit_log_filter_rotations;

--error ER_WRONG_TYPE_FOR_VAR
SET GLOBAL audit_log_filter_rotations=0.5;
SELECT @@global.audit_log_filter_rotations;

--error ER_WRONG_TYPE_FOR_VAR
SET GLOBAL audit_log_filter_rotations='str';
SELECT @@global.audit_log_filter_rotations;

--error ER_WRONG_TYPE_FOR_VAR
SET GLOBAL audit_log_filter_rotations='';
SELECT @@global.audit_log_filter_rotations;
